version: '3.8'
services:
  trilium:
    image: triliumnext/notes:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ~/trillium_data:/home/Trillium_Srv/trilium-data
    environment:
      - TRILIUM_DATA_DIR=/home/Trillium_Srv/trilium-data
